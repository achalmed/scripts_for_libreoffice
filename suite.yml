# suite.yml — suite document_studio: Interfaz de escritorio (PySide6) sobre docflow, pdf-suite y page-counter.
# Contrato: core/suite.schema.yml; core/suites.py validar lo comprueba y generar escribe los índices y README.
id: document_studio
objetivo: documentos
resumen: Interfaz de escritorio (PySide6) sobre docflow, pdf-suite y page-counter.
escribe_en:
- archivos
simula_por_defecto: false
comandos:
- main.py
- './run.sh                 # usa el .venv si existe'
- python3 -m studio
estado: activo
interfaz: gui
depende_de:
- PySide6
nota: config en studio/core/settings.py (QSettings); backends docflow, pdf-suite y page-counter en backends/
