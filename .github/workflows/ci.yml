# CI de docflow: lint (shellcheck) + suite Bats en Ubuntu y Arch Linux.
name: CI

on:
  push:
    paths: ["docflow-studio/backends/docflow/**"]
  pull_request:
    paths: ["docflow-studio/backends/docflow/**"]

jobs:
  lint:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: shellcheck
        run: |
          sudo apt-get update -q && sudo apt-get install -y shellcheck
          shellcheck -x -S warning \
            docflow-studio/backends/docflow/bin/docflow \
            docflow-studio/backends/docflow/install.sh \
            docflow-studio/backends/docflow/lib/core/*.sh \
            docflow-studio/backends/docflow/lib/engines/*.sh \
            docflow-studio/backends/docflow/lib/formats/*.sh \
            docflow-studio/backends/docflow/lib/commands/*.sh

  test-ubuntu:
    name: Bats (Ubuntu)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Instalar dependencias
        run: |
          sudo apt-get update -q
          sudo apt-get install -y bats pandoc libreoffice qpdf ghostscript poppler-utils python3
      - name: Ejecutar suite
        run: bats docflow-studio/backends/docflow/tests/

  test-arch:
    name: Bats (Arch Linux)
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
      - name: Instalar dependencias
        run: pacman -Syu --noconfirm bash bash-bats pandoc-cli libreoffice-still qpdf ghostscript poppler python git
      - uses: actions/checkout@v4
      - name: Ejecutar suite
        run: bats docflow-studio/backends/docflow/tests/
